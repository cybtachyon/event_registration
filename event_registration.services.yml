# yaml-language-server: $schema=../../../../vendor/symfony/dependency-injection/Loader/schema/services.schema.json
services:
    event_registration.registration_manager:
        class: Drupal\event_registration\RegistrationManager
        arguments:
            - '@entity_type.manager'
